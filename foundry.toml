[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]

# Compiler and EVM
evm_version = "cancun"
solc_version = "0.8.24"

# Optimization
optimizer = true
optimizer_runs = 200
via_ir = true

# Metadata
bytecode_hash = "none"
cbor_metadata = false

[fuzz]
runs = 128
